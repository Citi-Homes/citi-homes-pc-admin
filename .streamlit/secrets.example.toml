[attendance_sso]
# Generate a long random value and copy it to .streamlit/secrets.toml.
# The same value must be configured in the Attendance Portal server-side verifier.
shared_secret = "replace-with-a-long-random-secret"
token_ttl_seconds = 300
